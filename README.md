# Here are your Instructions
Open the terminal and : cd frontend
Then : npm install
Then : npm run start
open another terminal and : cd backend
Then : python -m uvicorn server:app --reload
if uvicors isn't there then install it
Backend on port : 8000 
Frontend on port : 3002
run backend 1st then frontend
crtl+click the link, you'll have the site